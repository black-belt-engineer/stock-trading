/** Symbols used when the client does not specify a list and for discovery. */
export const DEFAULT_SYMBOLS = [
  "AAPL",
  "TSLA",
  "GOOGL",
  "MSFT",
  "AMZN",
  "NVDA",
] as const;

export const SSE_POLL_INTERVAL_MS = 500;
